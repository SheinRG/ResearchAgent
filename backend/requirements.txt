# AI Research Agent — Backend Dependencies

# --- Core Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Async HTTP ---
httpx==0.28.1

# --- LangGraph Agent Orchestration ---
langgraph==0.2.60
langchain-core==0.3.28

# --- Cloud LLM (Groq) ---
groq==0.25.0

# --- Web Scraping ---
trafilatura==2.0.0

# --- Re-ranking (CPU-only) ---
flashrank==0.2.9

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# --- Redis Cache ---
redis[hiredis]==5.2.1

# --- Auth ---
PyJWT==2.10.1
bcrypt==4.2.1

# --- File Processing ---
pypdf==4.3.1
python-docx==1.1.2
python-multipart==0.0.20

# --- Utilities ---
python-dotenv==1.0.1

# --- Observability (optional; active only when SENTRY_DSN is set) ---
sentry-sdk[fastapi]==2.20.0

# --- LLM tracing (optional; active only when LANGFUSE_*_KEY are set) ---
# Pulls in the OpenTelemetry SDK. Adds ~25MB RSS, which matters on the 512MB
# Render starter plan the blueprint targets — see render.yaml.
langfuse==4.14.2

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
